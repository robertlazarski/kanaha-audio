# Axis2/C Module Configuration for Kanaha Audio
# JSON-RPC over HTTP/2 + mTLS — AudioSearchService

<IfModule axis2_module>
    # Repository (contains axis2.xml, services/, modules/), relative to ServerRoot
    Axis2RepoPath "axis2c"
    Axis2LogFile "logs/axis2.log"
    Axis2LogLevel info
    Axis2MaxLogFileSize 10485760
    Axis2GlobalPoolSize 16777216
    Axis2ServiceURLPrefix "/services"
</IfModule>

<Location "/services">
    SSLRequireSSL
    SSLVerifyClient require
    Require all granted
    SetHandler axis2_module
    Header always set Cache-Control "no-cache, no-store, must-revalidate"
</Location>
